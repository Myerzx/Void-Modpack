{
  "name": "voidfall-platform",
  "version": "0.0.0",
  "private": true,
  "description": "VoidFall management platform monorepo",
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "npm@11.11.0",
  "engines": {
    "node": ">=24.11.0 <25",
    "npm": ">=11.11.0 <12"
  },
  "volta": {
    "node": "24.18.0",
    "npm": "11.11.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build:apps",
    "build:apps": "npm run build --workspace @voidfall/control-api && npm run build --workspace @voidfall/build-worker && npm run build --workspace @voidfall/server-agent && npm run build --workspace @voidfall/panel-web",
    "build:packages": "npm run build --workspace @voidfall/contracts && npm run build --workspace @voidfall/mod-catalog && npm run build --workspace @voidfall/artifact-quarantine && npm run build --workspace @voidfall/authorized-files && npm run build --workspace @voidfall/configuration-schemas && npm run build --workspace @voidfall/modpack-release && npm run build --workspace @voidfall/permissions && npm run build --workspace @voidfall/authentication && npm run build --workspace @voidfall/database && npm run build --workspace @voidfall/minecraft-process && npm run build --workspace @voidfall/server-backup && npm run build --workspace @voidfall/server-configuration",
    "check": "npm run build:packages && npm run typecheck && npm run test && npm run build:apps",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "tsx": "4.23.5",
    "typescript": "6.0.3"
  }
}
